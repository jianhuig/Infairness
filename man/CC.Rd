% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CC.R
\name{CC}
\alias{CC}
\title{Control-corrected semi-supervised fairness estimation.}
\usage{
CC(
  Y,
  S,
  A,
  threshold = 0.5,
  X = NULL,
  basis = "Spline Interaction",
  nknots = 3,
  D = FALSE,
  kernel_order = 0.45,
  kernel_use_ecdf = FALSE,
  return_imputation = FALSE,
  ...
)
}
\arguments{
\item{Y}{Outcome, with missing values for unlabeled observations.}

\item{S}{Model score.}

\item{A}{Group indicator.}

\item{threshold}{Threshold for classification based on the model score.
Default value is 0.5. Can be a scalar or one value per observation.}

\item{X}{Optional covariates matrix used by \code{"Spline(S) + X"} and
\code{"Spline Interaction"}.}

\item{basis}{Character vector giving the imputation basis within each group.
If a single value is supplied, it is reused for every group. Supported
values are \code{"Spline(S)"}, \code{"Spline(S) + X"}, \code{"Spline Interaction"},
\code{"Beta"}, and \code{"kernel"}.}

\item{nknots}{Number of knots for spline-based branches. Default is 3.}

\item{D}{Logical; if \code{TRUE}, include the threshold indicator
\code{I(S > threshold)} in spline design matrices. Default is \code{FALSE}.}

\item{kernel_order}{Bandwidth exponent used by the kernel smoother.
Default is 0.45.}

\item{kernel_use_ecdf}{Logical; if \code{TRUE}, transform \code{S} to the within-group
empirical CDF scale before fitting the kernel smoother.}

\item{return_imputation}{Logical; if \code{TRUE}, return fitted imputations.}

\item{...}{Reserved for future compatibility.}
}
\value{
A list containing \code{est}, \code{var}, \code{alpha}, and \code{cv_weights}. If
\code{return_imputation = TRUE}, the list also contains \code{m_labeled} and \code{m_all}.
}
\description{
\code{CC()} is a standalone control-variate estimator. It fits an imputation
model for \code{E(Y | S, X, A)} within each group, then uses labeled-data
supervised metrics corrected by the labeled-vs-full imputation contrast.
}
